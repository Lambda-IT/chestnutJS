{
  "name": "chestnutjs",
  "version": "1.5.0",
  "description": "",
  "files": [
    "dist/**/*"
  ],
  "keywords": [],
  "author": "",
  "license": "MIT",
  "scripts": {
    "build:server": "cd ./server && npm run build:npm",
    "build:client": "cd ./client && npm run build:npm",
    "build": "npm run build:server && npm run build:client",
    "prepublishOnly": "shx rm -rf dist && cd ./server && npm install && cd .. && tsc -p tsconfig.publish.json && cd ./client && npm install && npm run build:npm"
  },
  "devDependencies": {
    "shx": "0.3.2"
  }
}
